# 开发规划 — 语音指令控制台 MCP

> 周期：5-7 天（建议一个 AI 连续 2 天攻坚 + 1 天打磨）。可选迭代：用独立 agent 分阶段。

## Phase 1（1-2 小时）：MCP Server 骨架
**目标**：可被任意 MCP 客户端（Claude Desktop/Cursor/自写）调用。
- [ ] 注册 5 个工具（见规范 §3）
- [ ] `mcp` SDK 的 transport（stdio）跑通
- [ ] `tests/test_conn.py` 绿
- 验证：启动 server，用 `mcp inspector` 或自己的 Python client 调用 `speak("你好")` 成功

## Phase 2（3-4 小时）：语音输入闭环
**产出：stt.py**
- [ ] faster-whisper 加载 base 模型（首次下载 ~150MB 注明）
- [ ] 热键录音（`keyboard`）→ 麦克风 → 文本
- [ ] 无 key 自动离线；带 `OPENAI_API_KEY` 走 API
- [ ] 四五句测试语料人工验证识别率
- 验证：按热键说"打开桌面"，终端输出文本

## Phase 3（4-5 小时）：执行与确认
- [ ] `run_cli` 白名单/黑名单（规范 §5）
- [ ] "确认？"→ 语音应答确认流程（含超时取消）
- [ ] 命令输出摘要（stdout 顶 N 行 + 错误提炼）
- [ ] `tests/test_safety.py` 全绿
- 验证：白名单 `ls` 放行、`rm -rf` 拒绝、`open_folder` 需确认后才执行

## Phase 4（3 小时)：TTS 播报 + 打包
- [ ] edge-tts 中文播报，`speak` 工具可用
- [ ] TTS 播报执行结果："已打开，路径是 xxx"
- [ ] `config.json` 全部配置生效、README、示例录屏脚本
- 验证：说一句话 → 听完成回读 完整闭环 30 秒内

## 里程碑验收清单
- [ ] 热键说话 → 白名单命令执行 → 播报结果（一条链路 30 秒内）
- [ ] 危险命令拒绝（日志可见）
- [ ] 无 LLM key 环境下 STT 仍可用（本地 whisper）
- [ ] 全部 pytest 绿

## 风险与对策
| 风险 | 对策 |
|---|---|
| 本地 whisper 安装重 | 提供 pip install 一条命令 + 首次下载提示 |
| 误执行命令 | 默认严格白名单 + 确认钩子 + 黑名单 |
| 语音识别不准 | 高置信才执行，低置信播报"听不懂请重复" |