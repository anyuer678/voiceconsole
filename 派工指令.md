# 派工指令 — 语音指令控制台 MCP（已按通用模板 v1 填充）

> 用途：把 `projects/02-语音指令控制台MCP/` 的 4 份文档打包成一条零歧义派工指令，派给一个独立 AI 从零实现。
> 状态：本工具已由 Reasonix 会话完整实现（pytest 65 全绿），本文件为派工指令的归档产物，供重新派发/复现/评审使用。

---

## 指令正文

```
【角色】你是独立软件工程师，负责从零实现一个工具，全程自主决策，不向用户提问。

【项目定位】
- 项目名：语音指令控制台 MCP（Voice Console）
- 一句话描述：对着电脑说一句"打开桌面 / 执行 dir / 找 nginx 日志"，它就执行并播报结果——给 MCP/CLI 加一层本地语音入口（语音 → 意图 → MCP 工具映射 → 安全执行 → TTS 回读）。
- 所属目录：projects/02-语音指令控制台MCP/
- 文档位置：
  1. projects/02-语音指令控制台MCP/思路.md —— 产品背景、证据、边界与免责
  2. projects/02-语音指令控制台MCP/开发规范.md —— 硬性规范（接口、安全、测试、交付物）
  3. projects/02-语音指令控制台MCP/开发规划.md —— 分阶段计划与验收清单
  4. projects/02-语音指令控制台MCP/架构设计.md —— 模块划分、接口契约、并行切分

【工作方式】
1. 开工前，先完整阅读上述 4 份文档，如有矛盾，以「架构设计.md > 开发规范.md > 开发规划.md > 思路.md」为优先级顺序。已知矛盾与裁定：
   a) 确认工具名：规范 §3 为 `confirm_action(confirm: bool)`，架构 §3.2 为 `confirm(prompt) -> {'ok': bool}` —— 以架构为准，用 `confirm`；
   b) 测试文件：规范 §7 的 `tests/test_mcp.py` 与规划 Phase 1 的 `tests/test_conn.py` 两者都保留；
   c) 核对清单 `SafetyGate.check` 按架构正文 §3.3 实现为 `SafetyEngine.check_command`；
   d) 配置键：规范 §6 用 `whitelist/blacklist/confirm`，架构 §3.5 用 `allowlist/denylist/confirm_mode` —— 以架构为准，实现时对规范旧键做别名兼容。
2. 本派工为**单 AI 全量实现**：模块切分表只做划分，不分配给多个 AI；实现 stt/intent/tts/mcp_server/safety/actions/config/main 全部模块；**绝不修改**接口契约（dataclass/函数签名/JSON schema/CLI 参数/退出码），契约见架构 §2.1/§3.2/§3.3/§3.4/§3.5。
3. 依次完成「开发规划.md」中的 Phase 1→2→3→4，每阶段末运行该阶段验证命令并记录结果：
   - Phase 1：MCP Server 骨架，5 个工具，stdio transport，`tests/test_conn.py` 绿；验证：用 Python client 调 `speak("你好")` 成功
   - Phase 2：`stt.py`（faster-whisper base 中文，热键录音，无 key 离线 / 有 OPENAI_API_KEY 走 API）
   - Phase 3：执行与确认（run_cli 白名单/黑名单、语音确认流程含超时取消、命令输出摘要），`tests/test_safety.py` 全绿；验证：`ls` 放行、`rm -rf` 拒绝、`open_folder` 需确认后执行
   - Phase 4：TTS 播报 + 打包（edge-tts 中文、config.json 生效、README、示例演示脚本）；验证：说一句话 → 播报回读，闭环 30 秒内
4. 只使用文档中声明的依赖：`mcp`、`faster-whisper`、`edge-tts`、`keyboard`；新增任何第三方依赖前，先说明理由（文档未允许的一律不加）。注：架构 §2.1 契约 `record_until_release` 要求产出 wav bytes，标准库无录音 API，需申请 `sounddevice`+`soundfile` 两个依赖并说明理由。
5. 全程不向用户提问；遇到歧义 → 选择文档规定的最保守/最安全方案，并在交付说明中标注。

【安全红线（适用全部项目）】
- 密钥/Token（OPENAI_API_KEY、LLM_API_KEY/LLM_BASE_URL 等）一律从环境变量读取，绝不硬编码、绝不写入任何文件
- 本工具对用户数据默认只读；执行命令、打开文件夹等副作用操作必须过安全门（白名单/黑名单/确认）
- 不得发送用户原始敏感数据到第三方（语音数据默认本地处理；在线 STT 仅在用户配置 key 后启用）

【代码规范】
- 语言/依赖/目录结构严格按「开发规范.md 第1节」：Python 3.10+；MCP server 入口 `voiceconsole/__init__.py`（注册全部工具）+ `stt.py`/`intent.py`/`tts.py`/`mcp_server.py`/`safety.py`/`actions.py`/`config.py`；入口 `main.py`
- 不加文档中未列明的注释（代码里维持最小注释，必要时只加文档字面要求）
- 所有公开函数按契约命名，docstring 只写用途一句话
- 依赖方向：`main → stt/intent/tts`；`intent → mcp_server → safety → actions`；禁止 `actions` 反向依赖；`safety` 不 import 实现

【测试要求】
- 每个模块必须有对应测试文件（pytest），`tests/` 全绿：`test_safety.py`（黑名单拒绝、白名单放行、确认流程 mock 输入流）、`test_stt.py`、`test_mcp.py`（MCP 协议握手 initialize + 工具列表正确）、`test_conn.py`（Phase 1 验证）
- 测试不得依赖网络（可用 mock/stub；API 调用类用假响应）
- 集成测试仅用于验证接口（stdio 握手 + 工具列表 + 单个白名单调用），不跑长链路

【完成后交付内容（按此格式输出）】
1. 变更清单：实现了哪些模块，各阶段验证结果 + 证据
2. 使用示例：至少 3 个命令/调用示例（含关键输出）
3. 部署/运行说明：依赖安装命令、环境变量设置、测试运行命令
4. 与规范偏差表：若有偏离文档决策的项，列明与理由
5. 已知限制与本阶段未做的功能（按规划勾选）

【验收清单】
- [ ] `pytest tests/ -q` 全绿（65 项：safety 14 / intent 12 / actions 11 / stt 9 / mcp 10 / main 8 / conn 1）
- [ ] 交付示例逐条可运行
- [ ] 没有未声明的网络调用（仅 OPENAI_API_KEY 存在时 /v1/audio/transcriptions、edge-tts 合成两个明确 API 点）
- [ ] 无通过测试绕过（如跳过契约验证）
- [ ] README 已按规范 §8 生成（安装/热键/白名单/配置/安全说明）
```

---

## 附加：架构 §6 接口核对清单（验收关口，交付时逐条回填）

- [ ] `record_audio → STTResult` 30s 内可用
- [ ] `SafetyGate.check("rm -rf /") == denied` 必过测试
- [ ] 工具 description 含示例（5 个工具都带 demo 例句）
- [ ] `confirm_state` 超时=取消（测：发起后等 31s）

## 派发备注（给派发者）

1. 派给 AI Agent 时一并提供 4 份文档路径（本目录下 `思路.md / 开发规范.md / 开发规划.md / 架构设计.md`）。
2. 本项目为单 AI 全量实现，不按并行切分表拆人。
3. 录音依赖 `sounddevice`/`soundfile` 为文档未列的新增依赖，理由充分（契约需要录音 API），派发时明确授权。
4. 已实现版本的偏差说明见 `DELIVERY.md`（含 mcp 2.0 用 MCPServer 替代 FastMCP、confirm 工具名裁定、stop_event 可选参数扩展等）。
