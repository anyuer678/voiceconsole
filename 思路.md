# 语音指令控制台 MCP（Voice Console）

> 一句话：对着电脑说"打开那个昨天改过的项目"，它就执行；本质是给 MCP/CLI 加一个本地语音入口。

## 一、项目思路

### 1. 要解决的问题
- AI Agent（Claude/Copilot/自定义 agent）火，但**人的输入还是打字**。语音是"AI 工具"转向"AI 助手"的必要入口
- 现有 demo 都止步于"说→转文字→回显"，没有"说→执行工具→回报结果"的完整闭环

### 2. 调研证据（2026年8月实搜）
- voice-mcp、voicelayer-mcp、simple-voice-mcp 等全部是 **demo 级**，无产品化实现
- OpenAI 官方 Cookbook 只有"语音+Agent 模式"的建议示范，没有可运行成品
- MCP 协议（Model Context Protocol）是 2025-2026 最热赛道，语音 MCP 是真空地带

### 3. 用户画像
- 独自开发/自托管玩家、走路时想操作设备的懒人、无障碍用户
- 任何"手上正忙、嘴里能说"的场景：吃饭时关服务器？不行——那适合运维场景（手上事多、没法敲命令）

### 4. 产品形态（MVP）
本地常驻服务（Python）+ 语音客户端（热键 PTT 对讲机模式）：
```
短按热键说："打开 nginx 日志"
→ 本地 STT（whisper 本地版或在线 API）→ MCP 工具调用 → 执行/授权确认 → 播报结果（TTS）
```

核心价值定位：**给任何 MCP Server 和 CLI 换语音皮肤**——用户已装了一堆 MCP server（文件、浏览器、笔记本），这个项目是"口译层"。

### 5. 核心杀手锏（差异化）
1. 一次说成整服或拆穿：整个"语音→意图→MCP 工具映射→执行"链路闭环
2. **安全确认层**：破坏性操作（删除/覆盖）必须语音确认二遍；白名单工具免确认
3. 本地优先：语音数据默认不出本机（Whisper 本地可选）

### 6. 边界与免责（低责任）
- 可执行的工具范围默认限制（白名单 + 危险命令黑名单）
- TTS/STT 若在线需用户自配 key（如 OpenAI Realtime API / 讯飞）
- 不处理资金、邮件发送等敏感自动化（预留钩子但默认关）

## 二、技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.10+ | MCP SDK 官方支持 |
| STT | faster-whisper（本地，可选）或 OpenAI/讯飞在线 API | 本地优先，不强制账号 |
| MCP | 官方 `mcp` Python SDK | 生态标准 |
| TTS | edge-tts（免费本地）或系统 TTS | 低依赖 |
| 触发 | 系统热键（keyboard 库）或二级确认 cli | 简单可汉堡 |

## 三、开发规范

见 `开发规范.md`：MCP Server 结构、安全白名单、确认流程、工具注册。

## 四、开发规划（见 开发规划.md）

- **Phase 1**：MCP Server 骨架 + 2 个示例工具（open-cli、read-file）+ 文字命令测试
- **Phase 2**：语音输入闭环（热键→STT→工具调用）
- **Phase 3**：安全确认、白名单、TTS 播报、打包发布

## 五、判断标准
- 说一句话能执行一个有意义的操作（如"找上周三改过的文件"）
- 危险命令必须二次确认
- 全程本地可跑（在线 key 可选）