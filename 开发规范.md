# 开发规范 — 语音指令控制台 MCP

## 1. 总体约束
- **语言**：Python 3.10+
- **依赖**：`mcp`（官方 SDK）、`faster-whisper`（STT）、`edge-tts`（TTS）、`keyboard`（热键）
- **结构**：一个 MCP server 入口 `voiceconsole/__init__.py`（注册全部工具）+ `stt.py`/`tts.py`/`safety.py`
- **禁止**：无授权执行敏感命令、密钥硬编码

## 2. MCP 协议（按官方示例实现）
- `initializeTool`：返回工具列表
- 每个工具声明 `description` 让 LLM 听声辨义
- 工具实现用 `@server.tool()` 装饰器注册

## 3. 工具清单（MVP 必含）
| 工具 | 输入 | 说明 |
|---|---|---|
| `run_cli` | `command: str` | 执行 shell 命令（白名单内），返回 stdout/stderr 摘要 |
| `find_file` | `pattern: str`, `dir: str` | 搜索文件（模糊名字） |
| `open_folder` | `path: str` | 打开资源管理器 |
| `speak` | `text: str` | TTS 播报（供工具回读） |
| `confirm_action` | `confirm: bool` | 二段确认的安全钩子（见 §5） |

## 4. STT/TTS 规范
- 默认本地：`faster-whisper` `base` 模型（中文 `zh`），热启动缓存
- 可选在线：`OPENAI_API_KEY` 时用 Realtime/transcription；无 key 自动退本地
- 触发：全局热键 Ctrl+Shift+Space 录音开始/结束
- 误识容错：同句 3 秒内无 action 判定失败，播报"没听懂，请再说一次"

## 5. 安全（硬性）
- **白名单执行**：`run_cli` 只执行 commander 表中允许的命令前缀（`cd/ls/cat/ping/ps/top/git status/...`），其余拒绝
- **危险命令黑名单**（拒绝执行）：`rm/sudo/curl|bash/格式化的磁盘命令/shutdown/密码相关`
- **二阶段确认**：白名单外工具 `open_folder` 等→ 先调用 `speak` 播报"确认执行：xxx？"，用户回"是/yes/对"才执行；30 秒超时自动取消
- 所有执行默认 `timeout=10s`，防挂起
- 敏感参数（含路径注入 `;` `&&` 等）校验拒绝

## 6. 配置
- `config.json`（可由 `--config` 指定）：`stt_engine: local|api`、`tts_engine`、`whitelist`、`blacklist`、`confirm`（全局开/关）
- 环境变量：`DEEPSEEK/OPENAI 类 key`（全部可选）

## 7. 测试
- `tests/test_safety.py`：黑名单拒绝、白名单放行、确认流程（mock 输入流）
- `tests/test_stt.py`（可选 llm）：循环目录下 wav → 文本
- `tests/test_mcp.py`：MCP 协议握手 `initialize` + 工具列表正确
- `pytest` 全绿为过关

## 8. 交付物
- 可运行 MCP server（`mcp.run()`）+ `main.py` 本地热键循环
- README（安装/热键/白名单/配置/安全说明）
- 演示脚本（speak 一句，调用 open_folder 的录屏可演示）