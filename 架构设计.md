# 架构设计 — 语音指令控制台 MCP

> 目标：语音链路与 MCP 工具层解耦；接口可单测；确认流程是独立状态机。

## 1. 总体分层

```
┌─────────────────────────────────────────────────┐
│  main.py         热键监听 + 全局循环（生命周期）   │
├──────────┬──────────────────┬───────────────────┤
│ stt.py   │   intent.py      │  tts.py          │
│ 语音转文本│  意图→工具映射   │  文本转语音      │
├──────────┴───────┬──────────┴───────────────────┤
│        mcp_server.py  (mcp SDK: 工具注册/执行)   │
├──────────────────┼──────────────────────────────┤
│      safety.py   │        actions.py            │
│  白名单/黑名单    │    真实执行体（CLI/FS）       │
│  确认状态机       │                              │
└──────────────────┴──────────────────────────────┘
```

依赖方向：`main → stt/intent/tts`；`intent → mcp_server → safety → actions`。
**禁止**：`actions` 反向依赖；`safety` 不 import 实现。

## 2. 接口契约

### 2.1 音频接口 `stt.py`
```python
@dataclass
class STTResult:
    text: str
    confidence: float          # 0~1，<0.55 视为不可执行
    language: str              # "zh"/"en"
    duration_ms: int

def init_stt(engine: str = "local", model: str = "base") -> None
    # engine ∈ {"local"(faster-whisper), "api"， 空则测环境变量自动判}
def transcribe(wav_bytes: bytes) -> STTResult     # 唯一调用点,内部回调识别器
def record_until_release(device_idx: int = 0, max_sec: int = 15) -> bytes
    # 开始/停止包在 bytes 里；main 处理 hotkey 循环调用
```

### 2.2 意图层 `intent.py`
```python
@dataclass
class Intent:
    raw_text: str
    action: str                 # 枚举: execute_cli/find_file/open_folder/quit/help
    args: dict[str, str]        # 工具名保留
    confidence: float

def parse_intent(text: str) -> Intent:
    """规则+落字典：(说"打开xxx""找xxx""执行 ls")；规则匹配失败返回 action="unknown"。
    若 LLM_MODE=1 可先用 LLM；默认规则即可 (无 LLM key 也能跑)"""

def map_to_tool(intent: Intent) -> tuple[str, dict] | None
    # Intent -> (mcp_tool_name, tool_args)
```

### 3.2 MCP 工具注册（`mcp_server.py`, 用官方 SDK 装饰器）
```python
@mcp.tool()  # name="run_cli", description 带 demo 例句
def run_cli(command: str, cwd: str | None = None) -> dict
    # 返回 {"stdout": str, "stderr": str, "exit_code": int, "elapsed_ms": int}
    # 内部: safety.check_command() 不过则 raise ToolDeniedError

@mcp.tool()  # "find_file"
def find_file(pattern: str, directory: str = ".") -> dict
    # {"matches": [{"path":..., "is_dir": bool, "mtime": ...}], "limit": ...20}

@mcp.tool()  # "open_folder"
def open_folder(path: str) -> dict        # {ok: True} / {ok: False, error}

@mcp.tool()  # "speak"
def speak(text: str) -> dict             # TTS 播报，{ok}

@mcp.tool()  # "confirm"
def confirm(prompt: str) -> dict         # {'ok': bool}（供安全确认）
```

### 3.3 确认状态机 `safety.py`
```python
DENY_PREFIXES = ("rm ", "shred", "dd ", "mkfs", "curl ", "wget ", "sudo ", "mv ", "del ")
ALLOW_PREFIXES = ("ls", "cd", "cat", "git status", "git log", "pwd", "where",
                  "systeminfo", "tasklist", "dir")
class SafetyEngine:
    def check_command(self, cmd: str) -> SafetyVerdict
        # -> allowed | denied | needs_confirm
    def needs_confirm(self, cmd: str) -> bool
    def start_confirm_flow(self, tool: str, args: dict) -> str   # 返回 confirm_id
    def resolve_confirm(self, confirm_id: str, answer: bool) -> bool  # 超时默认 False
```
状态机：`IDLE → REQUESTED(30s 超时) → APPROVED/DENIED`；线程安全（锁）。

## 3.4 actions.py（真执行，唯一调 subprocess 处）
```python
def run_cli_cmd(command: str, cwd: str | None, timeout_ms: int = 10_000) -> CLIResult
def open_in_file_manager(path) -> bool     # platform 分支: explorer/xdg-open/open
def search_files(pattern, directory, max_hits) -> list[dict]
def speak_text(text, engine) -> None        # edge-tts 或系统播报
```
所有函数**纯 IO**：调完不碰 MCP 对象。

## 3.5 `config.py`
```yaml
# config.json 结构与校验（"auto" 自动探测）
{"stt_engine": "auto|local|api", "tts_engine": "edge|system",
 "hotkey": "<ctrl>+<shift>+space", "allowlist": [..], "denylist": [..],
 "confirm_mode": "dangerous-only|all", "timeout_ms": 10000}
```
环境变量：`LLM_API_KEY`/`LLM_BASE_URL`（用于可选 LLM 意图模式），加载即校验类型。

## 4. 数据流（语音全链路）
```
hotkey 按下 → stt.record_audio → stt.recognize → intent.parse_intent
   → 若 unknown: tts.speak("没听懂") ；END
   → parse_to_tool → safety.check(tool) 
        allowed → actions.run → result → tts.speak(结果摘要)
        needs_confirm → tts.speak("确认执行xx?") → 收集语音"是/否" → resolve → 执行
```

## 5. 并行开发切分
- **AI-A**：`stt.py` + `tts.py` + 测试（录音→文本→播报 demo）
- **AI-B**：`safety.py`（纯逻辑；需求先定义 API,填桩）+ 测试
- **AI-C**：`actions.py` + 测试（危险命令拒批、白名单执行）
- **AI-D**：`intent.py` 规则引擎 + 测试
- **AI-E**：`main.py` 热键整合 + `mcp_server.py`（所有工具用上面桩对接）
依赖顺序：B/C → A → D → E。

## 6. 接口核对清单
- [ ] `record_audio→STTResult` 30s 内可用
- [ ] `SafetyGate.check("rm -rf /") == denied` 必过测试
- [ ] 工具 description 含示例 so LLM/意图命中率高
- [ ] `confirm_state` 超时=取消（测：发起后等 31s）